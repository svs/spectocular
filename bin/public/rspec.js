'use strict';

angular.module('svs.rspec', [])

// create a provider that loads the pusher script from a cdn
.Factory('RSpec', function () {

  this.setNode = function(name, stats, parent) {

  };

});
